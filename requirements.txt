# Core training (install torch matching your CUDA: https://pytorch.org)
torch>=2.4
numpy>=1.26
pyyaml>=6.0

# Tokenizer + data
transformers>=4.51   # Qwen3 support
datasets>=2.20
huggingface_hub>=0.24

# Post-training (SFT/DPO) — used in posttrain/, optional for pretraining
trl>=0.9
peft>=0.11

# Eval
# lm-eval @ git+https://github.com/EleutherAI/lm-evaluation-harness
